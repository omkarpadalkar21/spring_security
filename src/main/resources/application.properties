spring.application.name=spring_security

spring.security.user.name=omkar
spring.security.user.password=12345


spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=changemeinprod!
spring.jpa.hibernate.ddl-auto=update
# Explicitly set Hibernate dialect to avoid issues when JDBC metadata is not available
# (prevents: "Unable to determine Dialect without JDBC metadata")
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect